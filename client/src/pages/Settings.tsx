import { Badge } from "@/components/ui/badge";
import { BellRing, ShieldCheck, Wifi } from "lucide-react";
export default function SettingsPage() { return <div className="mx-auto max-w-3xl space-y-7"><div><p className="text-sm font-semibold text-[#2457d6]">SETTINGS</p><h1 className="mt-1 font-display text-3xl font-extrabold tracking-tight">Workspace settings.</h1><p className="mt-2 text-slate-500">Account controls and collaboration service status.</p></div><div className="overflow-hidden rounded-2xl border border-slate-200/80 bg-white shadow-sm"><Setting icon={ShieldCheck} title="Authentication" description="Your workspace session is protected by the managed sign-in service. Account credentials and session security are handled outside the application." badge="Protected" /><Setting icon={BellRing} title="Notifications" description="Task and project notifications are available in the application bell. Mark items read from the notifications page at any time." badge="Enabled" /><Setting icon={Wifi} title="Real-time collaboration" description="The board is equipped for live Socket.IO refresh events. A persistent production connection requires always-on hosting rather than autoscaled request instances." badge="Configured" /></div></div>; }
function Setting({ icon: Icon, title, description, badge }: { icon: typeof ShieldCheck; title: string; description: string; badge: string }) { return <div className="flex gap-4 border-b border-slate-100 p-6 last:border-0"><div className="grid h-10 w-10 shrink-0 place-items-center rounded-xl bg-blue-50 text-[#2457d6]"><Icon className="h-5 w-5" /></div><div className="min-w-0 flex-1"><div className="flex items-center gap-2"><p className="font-semibold">{title}</p><Badge className="rounded-md border-0 bg-emerald-50 text-[10px] text-emerald-700">{badge}</Badge></div><p className="mt-1 max-w-xl text-sm leading-5 text-slate-500">{description}</p></div></div>; }
